# PRD 对比差异文档：第二页「林间友人来信」

> 对比对象：
> - **PRD 设计稿**：`爱丽丝梦游仙境森林系邀请函设计 (5).png`（1600×2848，竖屏手机比例）
> - **当前实现**：`index.html`（第二页 `.page2`，约 366-638 行）+ `index.js`（page2 配置与渲染逻辑）
>
> 分析方法：由于设计稿为图片，使用 macOS Vision 框架 OCR 提取文字及坐标，配合像素级颜色采样与 64×40 网格色块布局图分析。坐标均为相对整页的百分比（x, y 自左上角起算）。

---

## 一、PRD 设计稿结构还原（第二页）

### 1. 顶部 Hero 区（y 0% ~ 30%）
- **主标题**「林间友人来信」：x≈21.7%、y≈12.6%，宽 56.6%、**高 6.5%（非常大，约为整页高度的 1/15）**，金色装饰字（区域均色 `#D5C086`）
- **副标题**「森林深处，传来一封童话般的回信」：x≈22.2%、y≈20.3%，宽 54.3%，淡金色（区域均色 `#C5B381`）
- 页面**左右两侧及顶部有金色藤蔓/花纹装饰边框**（左右金边均色 `#D6C576`，至少延伸至 y≈55%）

### 2. 信纸卡片（y ~32% ~ 56%，x ~5% ~ 88%）
- **大幅米黄色信纸卡片**，占屏宽约 83%（均色 `#EBE1B9`）
- 卡片上部（y 33% ~ 42%）为留白/装饰区（OCR 无文字，推测为信纸花纹或空白抬头区）
- 卡片中部为**居中排版的信件抬头**：
  - 「林间友人来信」（y≈43.3%，宽 41.5%，居中）
  - 下方一行小装饰符号（y≈44.5%，OCR 识别为「。。令」，疑为分隔花纹）
  - 「森林深处，传来一封童话般的回信」（y≈47.5%，宽 46.3%，居中）

### 3. 消息区域（y ~54% ~ 70%）
- 米黄纸色消息卡（均色 `#E1DABD`），与上方信纸卡片色接近（两卡之间区域色 `#FBF9DD`，可能视觉上连为一体的大信纸）
- **左侧圆形角色头像**（约 x 24% ~ 40%，均色 `#F1E1AF` 偏亮）
- 头像右侧：**「林间小兔」**（x≈45.7%，y≈54.5%）名字 + **「刚刚」**（x≈75.7%，y≈54.6%）时间在行右侧
- 正文三行（y 59.3% ~ 64.2%，x≈40.6% 起）：
  > 我们已收到你的婚礼邀请，
  > 愿穿过月光与花海，赴约这
  > 一场林间之约。

### 4. 底部按钮（y ≈ 89.8%）
- **「查看全部来信」**：x≈42.1%、宽 16%，**水平居中**，金色（区域均色 `#CEB275`），尺寸较小（高约 1.9%），样式类似金色文字按钮/链接

### 5. 背景
- 森林绿背景：中部橄榄绿（`#979258`）→ 底部深橄榄绿（`#6D7A47`），左下角有深色树影（色块图左下角为 D 深色区）
- 右下角「豆包AI」水印（y≈96.8%）——**为生成工具水印，实现时忽略**

---

## 二、当前版本结构（index.html / index.js）

第二页 `.page2`（index.html:600-638，样式 366-536 行）：

1. **Hero**：`.p2-hero` = `title-border.png` 边框图 + 绝对定位文字标题（26px，`#f3e5bd`）+ 副标题（12px，`#c8b88a`）
2. **信件轮播** `.p2-carousel`：半透明绿色圆角面板（含 `‹` `›` 两个切换箭头 + 底部圆点指示器），内部为**横向布局**：
   - 左侧大圆形头像（`.p2-avatar-main`，宽 18%，金边）
   - 右侧信纸卡（背景图 `./assets/envelope/4c3d12fe9b8c032e3fbd9aea974cb998 copy.png`），内含小头像 + 名字 + 时间 + 正文
3. 数据来自 `index.js` 的 `pageConfig.page2Letters`（3 封信：林间小兔 / 森林松鼠 / 月光猫头鹰），点击箭头轮播切换
4. **无「查看全部来信」按钮**，无整页金边装饰

---

## 三、差异清单（按优先级排序，供 code agent 修改）

### P0 - 布局结构性差异

| # | 差异点 | PRD 设计稿 | 当前实现 | 修改建议 |
|---|--------|-----------|---------|---------|
| 1 | **信纸卡片尺寸** | 大幅信纸卡占屏宽 ~83%、页高 ~24%，纵向大卡 | 小信纸（与 18% 宽头像横排，卡整体偏小） | 重构 `.p2-stage` 为纵向布局：信纸卡改为全宽大卡（占屏宽 83-90%），取消左侧大头像与信纸横排 |
| 2 | **信件抬头缺失** | 信纸卡上部有居中抬头：「林间友人来信」+ 分隔花纹 + 「森林深处，传来一封童话般的回信」 | 信纸上直接是小头像+名字+时间的消息头，无居中抬头 | 在信纸卡顶部增加居中排版抬头区（标题 + 花纹分隔线 + 副题），内容可复用 `page2HeroTitle` / `page2HeroSub` |
| 3 | **消息区形态** | 头像在消息卡左侧偏上，名字/时间/正文在头像右侧（类聊天消息） | 头像+信纸横排，信纸内 header 为小头像+名+时间 | 消息区改为：左圆形头像（约卡宽 15-18%）+ 右侧内容列（第一行：名字 + 右侧时间；下方正文） |

### P1 - 交互差异

| # | 差异点 | PRD 设计稿 | 当前实现 | 修改建议 |
|---|--------|-----------|---------|---------|
| 4 | **轮播箭头/圆点** | 设计稿中无 `‹ ›` 箭头、无圆点指示器 | 有左右箭头 + 圆点指示器 | 移除或隐藏 `.p2-arrow` 与 `.p2-dots`；仅展示一封信（默认第一封「林间小兔」） |
| 5 | **「查看全部来信」入口** | 底部 y≈90% 有居中金色「查看全部来信」按钮 | 无此按钮 | 在轮播/信纸区下方新增居中金色文字按钮（色值参考 `#CEB275` 系金色，与现有 `.letter-btn` 风格统一）；点击行为后续可定义为展开全部来信列表/滚动 |
| 6 | **多封信的展示方式** | 单条消息 + 「查看全部」入口的列表式设计 | 3 封信靠箭头轮播切换 | `page2Letters` 数据结构保留，默认渲染第一封，其余通过「查看全部来信」查看（具体交互可先做简单列表/弹层，或暂时只渲染第一封） |

### P2 - 视觉样式差异

| # | 差异点 | PRD 设计稿 | 当前实现 | 修改建议 |
|---|--------|-----------|---------|---------|
| 7 | **主标题字号** | 标题占页高 6.5%（约 40-44px @ 375 宽视口），视觉分量大 | 26px | `.p2-hero-title` 字号提升至约 36-44px（或按视口高度百分比 `clamp()`），保持金色 `#f3e5bd` 附近 |
| 8 | **整页金色侧边装饰** | 页面左右两侧有金色藤蔓花纹边框延伸至页面中部 | 仅 hero 标题周围有 `title-border.png`，页面两侧无装饰 | 若有竖向金边素材可加在 `.page2` 两侧；无素材可暂不做（视觉增强项） |
| 9 | **信纸底色** | 米黄纸色 `#EBE1B9`（大卡实底质感） | 使用信纸背景图 `4c3d12fe9b8c032e3fbd9aea974cb998 copy.png` | 保留背景图亦可（图片本身即米黄信纸）；若重构大卡，可给 `.p2-letter-card` 补充底色 `#EBE1B9` 兜底 |
| 10 | **正文文字颜色** | 深棕墨色（信纸上的深色字） | `#5a4632` | 基本一致，无需修改 |

### 文案差异

| # | 差异点 | 说明 |
|---|--------|------|
| 11 | 正文换行 | PRD 为「愿穿过月光与花海，赴约这/一场林间之约。」（按卡宽自然换行）；index.js 中为手动 `\n` 换行。**文字内容本身一致**，改版后卡变宽，建议去掉手动换行让文字自然换行 |
| 12 | 水印 | PRD 右下角「豆包AI」水印为生成工具水印，**不要**在页面中实现 |

---

## 四、修改涉及文件

- `index.html`：第二页样式（约 365-537 行）与结构（约 600-638 行）
- `index.js`：`pageConfig.page2*` 配置（约 19-45 行）与第二页渲染逻辑（约 170-206 行）

## ★ 素材类差异处理原则（重要）

**头像图片、信纸背景图、装饰花纹等素材与 PRD 图案不一致属于正常现象，用户后续会自行替换素材。code agent 无需为素材差异做任何代码修改**，只需保证：
1. 布局结构、尺寸比例、位置与 PRD 一致（见 P0/P1 差异项）
2. 素材引用路径保持可配置（现有 `pageConfig` 机制），方便用户直接换图
3. 具体而言：`mainAvatar` / `avatar` 头像图、`page2LetterBg` 信纸图、花纹分隔装饰，均**只搭骨架、不追求像素还原**；信纸底色 `#EBE1B9` 可作为背景图缺失时的兜底色

## 五、注意事项与不确定性

1. **OCR 低置信度内容**：y=43.3% 的「今林间友人来信公」与 y=44.5% 的「。。令」为低置信度识别，结合位置推断为信纸上的居中抬头文字与装饰花纹符号，实现时以「林间友人来信」+ 花纹分隔线处理即可。
2. **两张卡片是否一体**：色块分析显示信纸卡（y 32-56%）与消息卡（y 54-70%）之间区域颜色接近白色（`#FBF9DD`），两者可能为一张连续的大信纸（上半是抬头、下半是消息）。实现时建议做成**一张大信纸卡**，内部纵向排：抬头区 → 花纹分隔 → 消息区，与 PRD 视觉最接近。
3. PRD 图仅对应第二页；第一页及其余页面不在本次对比范围。
4. 「查看全部来信」的点击交互 PRD 未给出后续页面，实现时可先做成锚点/占位，待补充设计稿。